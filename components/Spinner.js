import React from 'react';
import styles from './Spinner.module.css'; // Import the CSS Module

const Spinner = () => {
    return (
        <div className={styles.spinner}></div> // Use the styles from the CSS Module
    );
};

export default Spinner; 